<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="CalendarMapper">

	<select id="dateInfo"	parameterType="java.lang.String"
							resultType="kr.co.mt.calendar.CalendarDTO">
	select dayofmonth( now() ) to_date
		, dayofmonth( last_day( #{userWantMonth} ) ) last_date
		, dayofweek( DATE_FORMAT( now(), #{userWantMonth} ) ) first_day
		, dayofmonth( last_day( #{userWantMonth} ) ) + dayofweek( DATE_FORMAT( now(), #{userWantMonth} ) ) for_end
		, month( #{userWantMonth} ) this_month
		, year( #{userWantMonth} ) this_year
	from dual
	</select>

</mapper>
